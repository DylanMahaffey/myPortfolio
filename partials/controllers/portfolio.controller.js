"use strict";

(function(angular) {
	angular
		.module("application")
		.controller("portfolioController", function() {

                // this.restaurante;
                // this.college;
                // this.arena;
				//
				// if(this.restaurante) {this.college, this.arena = false}
				// else if(this.college) {this.restaurante, this.arena = false}
				// else if (this.arena){this.college, this.restaurante = false}
				// else {this.restaurante, this.college, this.arena = false;}
        });
}(window.angular));
